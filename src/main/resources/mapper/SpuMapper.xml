<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cnzakii.tiedyer.mapper.SpuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.cnzakii.tiedyer.entity.Spu">
        <id column="id" property="id" />
        <result column="category_id" property="categoryId" />
        <result column="title" property="title" />
        <result column="image" property="image" />
        <result column="base_price" property="basePrice" />
        <result column="sales" property="sales" />
        <result column="use_spec" property="useSpec" />
        <result column="create_time" property="createTime" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, category_id, title, image, base_price, sales, use_spec, create_time, deleted
    </sql>

    <select id="getInforByLable">
        select t_spu.id id, title goodsName, production_place productionPlace, image pictureAddress, sales, base_price price, t_spu_spec.values yardage,category_name lable
        from t_spu JOIN t_category JOIN t_spu_spec
        where t_category.id = t_spu.category_id and t_spu.id = t_spu_spec.spu_id
        ORDER BY t_spu.sales desc LIMIT 20;
    </select>
</mapper>
